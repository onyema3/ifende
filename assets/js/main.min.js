(function(){'use strict';var cursor = document.getElementById('cursor');var ring = document.getElementById('cursorRing');var isTouchDevice = ('ontouchstart' in window) || (navigator.maxTouchPoints > 0);if (cursor && ring && !isTouchDevice) { document.documentElement.classList.add('has-custom-cursor'); document.addEventListener('mousemove', function(e) { cursor.style.left = e.clientX + 'px'; cursor.style.top = e.clientY + 'px'; setTimeout(function() { ring.style.left = e.clientX + 'px'; ring.style.top = e.clientY + 'px'; }, 80); }); document.querySelectorAll('a, button, [role="button"]').forEach(function(el) { el.addEventListener('mouseenter', function() { cursor.style.width = '24px'; cursor.style.height = '24px'; ring.style.width = '56px'; ring.style.height = '56px'; ring.style.borderColor = 'var(--green)'; }); el.addEventListener('mouseleave', function() { cursor.style.width = '12px'; cursor.style.height = '12px'; ring.style.width = '36px'; ring.style.height = '36px'; ring.style.borderColor = 'rgba(33,161,78,0.5)'; }); });}var themeToggle = document.getElementById('themeToggle');if (themeToggle) { themeToggle.addEventListener('click', function() { var current = document.documentElement.getAttribute('data-theme') || 'dark'; var next = current === 'dark' ? 'light' : 'dark'; document.documentElement.setAttribute('data-theme', next); localStorage.setItem('ifende-theme', next); }); window.matchMedia('(prefers-color-scheme: light)').addEventListener('change', function(e) { if (!localStorage.getItem('ifende-theme')) { document.documentElement.setAttribute('data-theme', e.matches ? 'light' : 'dark'); } });}var prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;var reveals = document.querySelectorAll('.reveal');if (prefersReducedMotion) { reveals.forEach(function(r) { r.classList.add('visible'); });} else if ('IntersectionObserver' in window) { var ro = new IntersectionObserver(function(entries) { entries.forEach(function(e) { if (e.isIntersecting) { e.target.classList.add('visible'); ro.unobserve(e.target); } }); }, { threshold: 0.1 }); reveals.forEach(function(r) { ro.observe(r); });} else { reveals.forEach(function(r) { r.classList.add('visible'); });}var form = document.getElementById('contactForm');if (form) { form.addEventListener('submit', function(e) { e.preventDefault(); var btn = document.getElementById('submitBtn'); var msg = document.getElementById('formMsg'); btn.textContent = 'Sending...'; btn.classList.add('loading'); btn.setAttribute('aria-busy', 'true'); var name = (document.getElementById('fname').value + ' ' + document.getElementById('lname').value).trim(); var email = document.getElementById('femail').value; var subject = document.getElementById('fsubject') ? document.getElementById('fsubject').value : 'Portfolio Enquiry'; var message = document.getElementById('fmessage').value; var data = { _subject: 'Portfolio Enquiry: ' + subject, _replyto: email, name: name, email: email, subject: subject, message: message }; var fs = ifendeData.formspree || ''; var w3 = ifendeData.web3forms || ''; /* Try WP AJAX first */ var fd = new FormData(); fd.append('action', 'ifende_contact'); fd.append('nonce', ifendeData.nonce); fd.append('name', name); fd.append('email', email); fd.append('subject', subject); fd.append('message', message); fetch(ifendeData.ajaxUrl, { method: 'POST', body: fd }) .then(function(r) { return r.json(); }) .then(function(res) { if (res.success) { showSuccess(btn, msg); } else if (fs) { tryFormspree(data, fs, btn, msg); } else if (w3) { tryWeb3(data, w3, btn, msg); } else { doMailtoFallback(data, btn, msg); } }) .catch(function() { fs ? tryFormspree(data, fs, btn, msg) : doMailtoFallback(data, btn, msg); }); });}function tryFormspree(data, id, btn, msg) { fetch('https: method: 'POST', headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: JSON.stringify(data) }) .then(function(r) { r.ok ? showSuccess(btn, msg) : doMailtoFallback(data, btn, msg); }) .catch(function() { doMailtoFallback(data, btn, msg); });}function tryWeb3(data, key, btn, msg) { fetch('https: method: 'POST', headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: JSON.stringify(Object.assign({}, data, { access_key: key })) }) .then(function(r) { r.ok ? showSuccess(btn, msg) : doMailtoFallback(data, btn, msg); }) .catch(function() { doMailtoFallback(data, btn, msg); });}function doMailtoFallback(data, btn, msg) { var fd = new FormData(); fd.append('action', 'ifende_get_email'); fd.append('nonce', ifendeData.nonce); fetch(ifendeData.ajaxUrl, { method: 'POST', body: fd }) .then(function(r) { return r.json(); }) .then(function(res) { var email = (res.data && res.data.email) ? res.data.email : 'hello@ifende.com'; doMailto(data, email, btn, msg); }) .catch(function() { doMailto(data, 'hello@ifende.com', btn, msg); });}function doMailto(data, email, btn, msg) { var body = 'Name: ' + data.name + '\nEmail: ' + data.email + '\n\nMessage:\n' + data.message; window.location.href = 'mailto:' + email + '?subject=' + encodeURIComponent(data._subject) + '&body=' + encodeURIComponent(body); setTimeout(function() { showSuccess(btn, msg); }, 1000);}function showSuccess(btn, msg) { btn.textContent = 'Message Sent \u2713'; btn.classList.remove('loading'); btn.setAttribute('aria-busy', 'false'); if (msg) { msg.style.display = 'block'; msg.textContent = "Thank you! I'll get back to you shortly."; msg.setAttribute('role', 'status'); msg.setAttribute('aria-live', 'polite'); }}var cookieBanner = document.getElementById('cookieBanner');if (cookieBanner) { var consent = localStorage.getItem('ifende-cookie-consent'); if (!consent) { cookieBanner.style.display = 'block'; setTimeout(function() { cookieBanner.classList.add('visible'); }, 100); } var acceptBtn = document.getElementById('cookieAccept'); var dismissBtn = document.getElementById('cookieDismiss'); function closeCookieBanner(value) { localStorage.setItem('ifende-cookie-consent', value); cookieBanner.classList.remove('visible'); setTimeout(function() { cookieBanner.style.display = 'none'; }, 400); } if (acceptBtn) acceptBtn.addEventListener('click', function() { closeCookieBanner('accepted'); }); if (dismissBtn) dismissBtn.addEventListener('click', function() { closeCookieBanner('dismissed'); });}var faqItems = document.querySelectorAll('.faq-item');faqItems.forEach(function(item) { var btn = item.querySelector('.faq-question'); if (btn) { btn.addEventListener('click', function() { var isOpen = item.classList.contains('open'); faqItems.forEach(function(other) { other.classList.remove('open'); }); if (!isOpen) item.classList.add('open'); }); }});})();